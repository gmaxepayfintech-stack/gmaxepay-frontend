import React from 'react';

export const getHomePageComponent = (domain) => {
  // You can add logic here to return different home page components based on domain
  // For now, returning null will redirect to login
  return null;
};
